/**
 * Re-export so the rest of the app imports the router from `app/router`,
 * matching XpertOne's layout. The tree itself lives in `routes/app-routes.tsx`.
 */
export { router } from '@/routes/app-routes'
